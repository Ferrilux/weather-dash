import { StyledFooter } from "./styles";

export const Footer = () => {
    return (
        <StyledFooter>
            <p>This is a footer - to be completed later</p>
        </StyledFooter>
    );
};